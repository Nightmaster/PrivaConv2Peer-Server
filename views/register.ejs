<!DOCTYPE html>
<html>
	<head>
		<title><%= title + ' - ' %>S'enregistrer</title>
		<link rel='stylesheet' href='/stylesheets/style.css' />
		<meta name="author" content="Gael B."/>
		<meta charset="UTF-8"/>
	</head>
	<body>
		<% if ('undefined' !== typeof error)
		{ %>
			<p>Erreur: <%= error %></p>
		<% } %>
		<form method="post" action="/registration">
			<p>Nom</p>
			<input type="text" name="name" placeholder="Votre nom" required
				<% if('undefined' !== typeof name)
				{ %>
					value="<%= name %>"
				<% } %>
			/>
			<p>Prénom</p>
			<input type="text" name="firstname" placeholder="Votre prénom" required
				<% if('undefined' !== typeof firstname)
				{%>
					value="<%= firstname %>"
				<% } %>
			/>
			<p>Pseudonyme :</p>
			<input type="text" name="username" placeholder="Votre nom d'utilisateur" required
				<% if('undefined' !== typeof username)
				{%>
					value="<%= username %>"
				<% } %>
			/>
			<p>Adresse Email :</p>
			<input type="email" name="email" placeholder="email@exemple.fr" required
				<% if('undefined' !== typeof email)
				{ %>
					value="<%= email %>"
				<% } %>
			/>
			<p>Vérification de votre adresse Email :</p>
			<input type="email" name="email-verif" placeholder="email@exemple.fr" required 
				<% if('undefined' !== typeof email)
				{ %>
					value="<%= email %>"
				<% } %>
			/>
			<p>Mot de passe :</p>
			<input type="password" name="password" placeholder="mot de passe" required/>
			<p>Vérification du mot de passe :</p>
			<input type="password" name="password-verif" placeholder="mot de passe"  required/>
			<br/>
			<input type="submit" value="Validez"/>
		</form>
	</body>
</html>